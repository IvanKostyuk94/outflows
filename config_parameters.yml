# Outflows configuration
# Paths can also be set via environment variables:
#   OUTFLOWS_BASE_PATH, TNG_DATAPATH, OUTFLOWS_SIM_NAME

# Base path where output dataframes are stored
base_path: "/ptmp/mpa/ivkos/outflows"

# TNG simulation data location
tng_datapath: "/virgotng/universe/IllustrisTNG"
sim_name: "L35n2160TNG"

# HDF5 settings
hdf_key: "galaxies"
dir_prefix: "snap_"
df_name: "testing_df_"
hdf_ending: ".hdf5"
cutout_scale: "1.1"
